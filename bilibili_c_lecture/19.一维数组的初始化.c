#include<stdio.h>
int main(void)
{
	int c_score[5] = { 90,80,85,95,70 };//全部赋初值
	int ps_score[5] = { 90,80 };//部分赋初值，未赋初值的元素默认为0

	//省略长度赋初值，数组的长度（数组的元素个数）根据初值个数决定
	int mysql_score[] = { 90,80,85,95 };//数组的元素个数为4

	int arr[3] = { 90,80.5,95.5 };//arr[0]=90,arr[1]=80,arr[2]=95 此处变量类型为整型，不会取小数
	//int b[5];//没有赋初值，所有元素的值都是未知
	int c[5] = { 0 };//所有元素的值都为0
	return 0;
}